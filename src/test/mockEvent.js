const mockEvent = {
  target: {
    appendChild(imageElement) {
    },
    classList: {
      add(something) {
      },
    },
    dataset: {
      datasymbol: '',
    },
  },
};

export default mockEvent;